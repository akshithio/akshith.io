// src is used for icons that have multiple variations (often different dimensions)
// used in different pages, so the src specifies the route from where the icon is
// being requested

export interface IconParams {
  src: string;
}
